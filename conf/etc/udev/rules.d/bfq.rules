# Set BFQ as the scheduler.
# (There's currently no way to do so at kernel compile time.)
# From https://www.linux.org.ru/news/kernel/13581989
ACTION=="add|change", KERNEL=="sd[a-z]", ATTR{queue/scheduler}="bfq"
